# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# WASM build artifacts (built in CI)
cpp/build/

# Native test harness (cpp/tests, built with g++ - see cpp/tests/main.cpp)
cpp/solver_tests.exe
cpp/solver_tests
*.o

# Emscripten output. Committed builds go stale against the C++ silently, so
# treat public/wasm as generated: CI rebuilds it, and a missing module just
# makes the app fall back to the TypeScript solver.
public/wasm/*.js
public/wasm/*.wasm

# Test coverage
coverage/

# Environment
.env
.env.local
